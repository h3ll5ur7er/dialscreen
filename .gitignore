# Secrets — never commit Wi-Fi / API / OTA credentials
secrets.yaml

# Generated by ./build.sh local (validates the working tree instead of the pushed copy)
validate-local.yaml

# ESPHome build cache & artifacts
.esphome/
*.bin

# Demo recordings (host README/wiki videos via GitHub attachments, not in-repo)
*.mp4

# Python
__pycache__/
*.pyc

# OS junk
.DS_Store
Thumbs.db
desktop.ini
